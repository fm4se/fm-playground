services:
  alloy-diff:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alloy-diff-api
    ports:
      - "8080:8080"
    environment:
      API_URL: http://localhost:8000
